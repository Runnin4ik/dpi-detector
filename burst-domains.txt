# Test 6 targets: ordinary sites, not the blocked ones test 2 measures.
#
# The burst test asks a different question — does the act of connecting to this
# host N times in a row change the answer? — so it needs hosts that answer
# normally on the first connection and are not censored by address or SNI. Two
# ways a host fails that and has been dropped here: a blocked one fails every
# attempt for a reason the test does not measure (reg.ru, whose TLS is intercepted
# and answers UNKNOWN on every shape), and a host that only redirects measures a
# redirect instead of a handshake (smartape.ru answers a 301 to its www form, so
# the list carries the address the redirect lands on).
ezgame.su
amserg.com
www.smartape.ru
