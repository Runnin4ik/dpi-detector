name: Release Multiplatform Binaries

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # 1. Windows x86_64 (Windows 10, 11, Server)
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            name: dpi-detector-windows-x86_64.exe
            use_cross: false
            use_nightly: false

          # 1b. Windows 7 Legacy x86_64
          - os: windows-latest
            target: x86_64-win7-windows-msvc
            name: dpi-detector-windows-7-x86_64.exe
            use_cross: false
            use_nightly: true
          # 2. Ubuntu / Linux x86_64 (100% static musl)
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            name: dpi-detector-linux-x86_64
            use_cross: false

          # 3. ARM64 Routers & SBCs (Keenetic Hopper/Titan 2, Xiaomi AX3000T, Raspberry Pi)
          - os: ubuntu-latest
            target: aarch64-unknown-linux-musl
            name: dpi-detector-linux-arm64
            use_cross: true

          # 4. ARM32 Routers (Keenetic Titan 1, Hero, ASUS RT-AX58U, MikroTik ac)
          - os: ubuntu-latest
            target: armv7-unknown-linux-musleabihf
            name: dpi-detector-linux-armv7
            use_cross: true
          # 5. MIPS Little-Endian Routers (Keenetic Giga KN-1011, Viva, Xiaomi 3G/4A, MikroTik hEX)
          - os: ubuntu-latest
            target: mipsel-unknown-linux-musl
            name: dpi-detector-linux-mipsel
            use_cross: true

          # 6. MIPS Big-Endian Routers (TP-Link Archer C7, MikroTik hAP ac lite)
          - os: ubuntu-latest
            target: mips-unknown-linux-musl
            name: dpi-detector-linux-mips
            use_cross: true

          # 7. macOS Intel
          - os: macos-14
            target: x86_64-apple-darwin
            name: dpi-detector-macos-intel
            use_cross: false

          # 8. macOS Apple Silicon (M1-M4)
          - os: macos-14
            target: aarch64-apple-darwin
            name: dpi-detector-macos-arm64
            use_cross: false

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain (stable)
        if: "!matrix.use_cross && !matrix.use_nightly"
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Rust toolchain (nightly for win7 or cross)
        if: "matrix.use_cross || matrix.use_nightly"
        uses: dtolnay/rust-toolchain@nightly
        with:
          components: rust-src
      - name: Install musl-tools
        if: matrix.target == 'x86_64-unknown-linux-musl'
        run: sudo apt-get update && sudo apt-get install -y musl-tools

      - name: Install cross binary
        if: matrix.use_cross
        run: |
          curl -fsSL https://github.com/cross-rs/cross/releases/download/v0.2.5/cross-x86_64-unknown-linux-musl.tar.gz | tar -xz -C ~/.cargo/bin
          chmod +x ~/.cargo/bin/cross
      - name: Build with cross (MIPS & Cross targets)
        if: matrix.use_cross
        env:
          RUSTFLAGS: "--cfg rustix_use_libc -C force-unwind-tables=no"
        run: cross build --release --target ${{ matrix.target }}

      - name: Build with cargo (Windows 7 legacy)
        if: "matrix.target == 'x86_64-win7-windows-msvc'"
        env:
          RUSTFLAGS: "-C target-feature=+crt-static"
        run: cargo +nightly build -Z build-std=std,panic_abort --release --target ${{ matrix.target }}

      - name: Build with cargo (Windows 10/11)
        if: "matrix.target == 'x86_64-pc-windows-msvc'"
        env:
          RUSTFLAGS: "-C target-feature=+crt-static"
        run: cargo build --release --target ${{ matrix.target }}

      - name: Build with cargo (Native Unix & macOS)
        if: "!matrix.use_cross && runner.os != 'Windows'"
        env:
          RUSTFLAGS: "${{ matrix.target == 'x86_64-unknown-linux-musl' && '-C force-unwind-tables=no' || '' }}"
        run: cargo build --release --target ${{ matrix.target }}

      - name: Prepare binary artifact (Windows)
        if: runner.os == 'Windows'
        run: |
          New-Item -ItemType Directory -Force -Path dist
          Copy-Item target/${{ matrix.target }}/release/dpi-detector.exe dist/${{ matrix.name }}

      - name: Prepare binary artifact (Unix)
        if: runner.os != 'Windows'
        run: |
          mkdir -p dist
          cp target/${{ matrix.target }}/release/dpi-detector dist/${{ matrix.name }}
          chmod +x dist/${{ matrix.name }}

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}
          path: dist/*

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/') && !cancelled()
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-files
          pattern: dpi-detector-*
          merge-multiple: true

      - name: Copy installation scripts
        run: |
          cp install.sh release-files/
          cp install.ps1 release-files/

      - name: List artifacts to publish
        run: |
          ls -lh release-files/
      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release-files/*
          draft: false
          prerelease: true
          generate_release_notes: false
          body: ""
