diff -r a\Cargo.toml b\Cargo.toml
--- a\Cargo.toml	2026-09-18 02:16:02.000498500 +0300
+++ b\Cargo.toml	2026-09-18 02:16:02.061169700 +0300
@@ -1,13 +1,9 @@
-# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
-#
-# When uploading crates to the registry Cargo will automatically
-# "normalize" Cargo.toml files for maximal compatibility
-# with all versions of Cargo and also rewrite `path` dependencies
-# to registry (e.g., crates.io) dependencies.
-#
-# If you are reading this file be aware that the original Cargo.toml
-# will likely look very different (and much more reasonable).
-# See Cargo.toml.orig for the original contents.
+# Vendored: upstream `rustls-rustcrypto` 0.0.2-alpha as published on crates.io,
+# plus the patch in `PATCH.diff` (see `README-PATCH.md`). This header replaces
+# cargo's "automatically generated" notice, which pointed at a `Cargo.toml.orig`
+# that is not shipped here; everything below it is upstream's normalized
+# manifest except the `webpki` dependency and the two feature entries the patch
+# removes.
 
 [package]
 edition = "2021"
@@ -30,7 +26,6 @@
 ]
 license = "MIT OR Apache-2.0"
 repository = "https://github.com/RustCrypto/rustls-rustcrypto"
-resolver = "1"
 
 [dependencies.aead]
 version = "0.5.2"
@@ -139,22 +134,12 @@
 version = "2.1.0"
 default-features = false
 
-[dependencies.webpki]
-version = "0.102.0"
-default-features = false
-package = "rustls-webpki"
-
 [dependencies.x25519-dalek]
 version = "2"
 default-features = false
 
-[dev-dependencies.getrandom]
-version = "0.2"
-features = ["custom"]
-
 [features]
 alloc = [
-    "webpki/alloc",
     "pki-types/alloc",
     "aead/alloc",
     "ed25519-dalek/alloc",
@@ -167,7 +152,6 @@
 logging = ["rustls/logging"]
 std = [
     "alloc",
-    "webpki/std",
     "pki-types/std",
     "rustls/std",
     "ed25519-dalek/std",
Only in b: PATCH.diff
Only in b: README-PATCH.md
diff -r a\src\kx.rs b\src\kx.rs
--- a\src\kx.rs	2026-09-18 02:16:01.939481300 +0300
+++ b\src\kx.rs	2026-09-18 02:16:02.052824700 +0300
@@ -30,11 +30,15 @@
         let peer_array: [u8; 32] = peer
             .try_into()
             .map_err(|_| rustls::Error::from(rustls::PeerMisbehaved::InvalidKeyShare))?;
-        Ok(self
-            .priv_key
-            .diffie_hellman(&peer_array.into())
-            .as_ref()
-            .into())
+        let shared = self.priv_key.diffie_hellman(&peer_array.into());
+        // RFC 8446 §4.2.8.2: a peer key of low order yields the all-zero secret,
+        // and the handshake must abort rather than build keys from it. The ring
+        // and aws-lc-rs providers reject it; x25519-dalek only reports it, and
+        // upstream leaves the report unread.
+        if !shared.was_contributory() {
+            return Err(rustls::Error::from(rustls::PeerMisbehaved::InvalidKeyShare));
+        }
+        Ok(shared.as_ref().into())
     }
 
     fn pub_key(&self) -> &[u8] {
diff -r a\src\lib.rs b\src\lib.rs
--- a\src\lib.rs	2026-09-18 02:16:01.942560100 +0300
+++ b\src\lib.rs	2026-09-18 02:16:02.053370700 +0300
@@ -13,7 +13,6 @@
     clippy::cast_precision_loss,
     clippy::cast_sign_loss,
     clippy::checked_conversions,
-    clippy::from_iter_instead_of_collect,
     clippy::missing_errors_doc,
     clippy::mod_module_files,
     clippy::implicit_saturating_sub,
diff -r a\src\misc.rs b\src\misc.rs
--- a\src\misc.rs	2026-09-18 02:16:01.943076700 +0300
+++ b\src\misc.rs	2026-09-18 02:16:02.053937100 +0300
@@ -6,7 +6,11 @@
         const __LEN: usize = A.len() + B.len();
         const __CONCATENATED: &[$ty; __LEN] = &{
             let mut out: [$ty; __LEN] = if __LEN == 0 {
-                unsafe { core::mem::transmute([0u8; core::mem::size_of::<$ty>() * __LEN]) }
+                unsafe {
+                    core::mem::transmute::<[u8; core::mem::size_of::<$ty>() * __LEN], [$ty; __LEN]>(
+                        [0u8; core::mem::size_of::<$ty>() * __LEN],
+                    )
+                }
             } else if A.len() == 0 {
                 [B[0]; __LEN]
             } else {
diff -r a\src\verify\ecdsa.rs b\src\verify\ecdsa.rs
--- a\src\verify\ecdsa.rs	2026-09-18 02:16:01.935450300 +0300
+++ b\src\verify\ecdsa.rs	2026-09-18 02:16:02.048492800 +0300
@@ -1,9 +1,8 @@
 use der::Decode;
 use digest::Digest;
 use paste::paste;
-use pki_types::{AlgorithmIdentifier, InvalidSignature, SignatureVerificationAlgorithm};
+use pki_types::{alg_id, AlgorithmIdentifier, InvalidSignature, SignatureVerificationAlgorithm};
 use signature::hazmat::PrehashVerifier;
-use webpki::alg_id;
 
 macro_rules! impl_generic_ecdsa_verifer {
     (
diff -r a\src\verify\eddsa.rs b\src\verify\eddsa.rs
--- a\src\verify\eddsa.rs	2026-09-18 02:16:01.935450300 +0300
+++ b\src\verify\eddsa.rs	2026-09-18 02:16:02.049018200 +0300
@@ -1,6 +1,5 @@
-use pki_types::{AlgorithmIdentifier, InvalidSignature, SignatureVerificationAlgorithm};
+use pki_types::{alg_id, AlgorithmIdentifier, InvalidSignature, SignatureVerificationAlgorithm};
 use signature::Verifier;
-use webpki::alg_id;
 
 #[derive(Debug)]
 struct Ed25519Verify;
diff -r a\src\verify\rsa.rs b\src\verify\rsa.rs
--- a\src\verify\rsa.rs	2026-09-18 02:16:01.936457900 +0300
+++ b\src\verify\rsa.rs	2026-09-18 02:16:02.049561200 +0300
@@ -1,10 +1,9 @@
 use paste::paste;
-use pki_types::{AlgorithmIdentifier, InvalidSignature, SignatureVerificationAlgorithm};
+use pki_types::{alg_id, AlgorithmIdentifier, InvalidSignature, SignatureVerificationAlgorithm};
 use rsa::pkcs1::DecodeRsaPublicKey;
 use rsa::{pkcs1v15, pss, RsaPublicKey};
 use sha2::{Sha256, Sha384, Sha512};
 use signature::Verifier;
-use webpki::alg_id;
 
 macro_rules! impl_generic_rsa_verifer {
     (
